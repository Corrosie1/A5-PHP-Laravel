<?php

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

class CreateRolesTable extends Migration
{
    /**
     * Run the migrations.
     *
     * @return void
     */
    public function up()
    {
        Schema::create('roles', function (Blueprint $table) {
            $table->id();
            $table->string('name');
            /* ^
            bovenstaand maakt een kolom aan, binnen deze kolom zullen
            De namen van de rollen naar voren toe komen.

            Nu is het alleen nog nodig om de roles tabel te linken met de user tabel (Zodat je de rollen kan koppelen aan users)
            */
            $table->timestamps();
        });
    }

    /**
     * Reverse the migrations.
     *
     * @return void
     */
    public function down()
    {
        Schema::dropIfExists('roles');
    }
}
